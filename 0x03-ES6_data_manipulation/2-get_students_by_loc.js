const getStudentByLocation = (students,city) => {
  const studentsLocation = student.filter(
    (student) => student.location === city,
   );

   return studentsLocation;
};

export default getStudentsLocation;
};

export default getStudentsByLocation;
